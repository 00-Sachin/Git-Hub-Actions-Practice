name: Self-Hosted Runner Label Test

on:
  workflow_dispatch:

jobs:
  labeled-hardware-job:
    name: Run on My Labeled Machine
    runs-on: [self-hosted, my-linux-runner]
    steps:
      - name: Verify Label Selection
        run: |
          echo "=== Success ==="
          echo "Job successfully routed using custom label!"
          hostname
